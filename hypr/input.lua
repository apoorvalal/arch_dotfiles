-- Personal input overrides, loaded after Omarchy's defaults.
hl.config({
  input = {
    -- Treat Caps Lock as an extra Control key.
    kb_options = "ctrl:nocaps",

    repeat_rate = 40,
    repeat_delay = 600,

    touchpad = {
      natural_scroll = true,
      clickfinger_behavior = true,
      scroll_factor = 0.4,
    },
  },
})
